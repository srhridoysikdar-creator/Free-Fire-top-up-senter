// JavaScript functionality for Free Fire Top-Up Center\n\n// Package selection function\nfunction selectPack(package) {\n    // Logic to select a package\n    console.log('Package selected:', package);\n}\\n\n// Form validation function\nfunction validateForm(form) {\n    // Validate name\n    if (!form.name.value) {\n        alert('Name is required.');\n        return false;\n    }\n    // Validate package selection\n    if (!form.package.value) {\n        alert('Please select a pack.');\n        return false;\n    }\n    return true;\n}\\n\n// Submit order function with WhatsApp integration\nfunction submitOrder(form) {\n    if (validateForm(form)) {\n        const orderDetails = {\n            name: form.name.value,\n            package: form.package.value,\n        };\n        const whatsappMessage = `Hello, I would like to order the following package: ${orderDetails.package} for ${orderDetails.name}`;\n        const whatsappLink = `https://api.whatsapp.com/send?text=${encodeURIComponent(whatsappMessage)}`;\n        window.open(whatsappLink);\n    }\n}\\n// Example usage:\n// selectPack('Basic Pack');\n// const form = { name: { value: 'John Doe' }, package: { value: 'Basic Pack' } };\n// submitOrder(form);